fleet-infrastructure-leased/spoke-configs/
cluster-totebox-personnel/*.img
media-marketing-landing/app-mediakit-telemetry/outbox/RAW_*.csv
media-marketing-landing/app-mediakit-telemetry/outbox/REPORT_*.md

# Canonical-mirror repos never carry real per-archive .agent/ state — that lives in
# clones/<archive>/.agent/ only. Untracked .agent/ residue here previously caused
# false-positive CRITICAL drift alerts in bin/foundry-health.sh (root-caused 2026-07-09).
.agent/
